# ─────────────────────────────────────────────────────────────────────────────
# LOCAL DEVELOPMENT — full feature set
#   pip install -r requirements-local.txt
# Includes the original Streamlit UI (app.py) plus the heavier optional
# NLP/ML libraries. These are NOT installed on Vercel.
# ─────────────────────────────────────────────────────────────────────────────
-r requirements.txt

streamlit>=1.28.0
plotly>=5.15.0
scikit-learn>=1.3.0
nltk>=3.8.1
textblob>=0.17.1
openpyxl>=3.1.0
Pillow>=10.0.0
pytest>=7.4.0
